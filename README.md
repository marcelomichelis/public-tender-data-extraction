# public-tender-data-extraction
Hybrid automation suite for extracting public tender data. Uses Python for HAR network analysis and JavaScript injection for DOM traversal, bypassing server-side obfuscation to retrieve dynamic content.
